# Replace "projectname" and "username"
image:
  name: hashicorp/terraform:light

before_script:
  - terraform version
  - go version

stages:
  - validate

validate:
  stage: validate
  script:
    - cd src/main/terraform/
    - terraform validate
